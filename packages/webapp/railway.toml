[build]
installCommand = "npm install -g pnpm@10 && cd ../.. && pnpm install --frozen-lockfile"
buildCommand = "cd ../.. && pnpm --filter @deal-quest/webapp build"
watchPatterns = ["packages/webapp/**", "packages/shared/**"]

[deploy]
startCommand = "serve dist -s -l tcp://0.0.0.0:${PORT:-8080}"
